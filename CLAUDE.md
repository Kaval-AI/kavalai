# Kaval.AI — Claude Code Guide

`AGENTS.md` at the repository root carries the same conventions in the
tool-agnostic form other coding agents read. Keep the two in step.

Before making a structural change, read `docs/tutorials/architecture.rst` — it
states the design commitments, the invariants that follow, and where each kind
of change belongs.

## Project Overview

Kaval.AI is a YAML-based AI agent framework with two main components:
- **kavalai**: Core SDK/runtime running on client infrastructure (agent logic, workflows, own DB); the runtime modules (`agent.py`, `db.py`, `server.py`, …) live directly in the top-level package.
- **kavalai.backoffice**: Management UI for configuring and monitoring agents.

Uses `loguru` for all logging and prefers f-strings for formatting.

## Working Copy & Git Workflow

- **Never use git worktrees.** Do not run `git worktree add`, do not use `EnterWorktree`,
  and do not launch agents or workflows with `isolation: "worktree"`. Worktree branches are
  invisible in the normal PyCharm window, which makes diffs impossible to review.
- Make all edits directly in the checked-out branch of `/home/timo/projects/kaval.ai`.
- Leave changes **uncommitted** so they can be reviewed in PyCharm's Local Changes view;
  never commit, branch, or push without asking first.
- This rule holds for background jobs too. `.claude/settings.json` sets
  `"worktree": {"bgIsolation": "none"}`, which switches off the harness guard that would
  otherwise force background sessions into a worktree — do not remove it.
- If a tool still insists on worktree isolation, do not leave the work there: copy the
  changed files back into `/home/timo/projects/kaval.ai` (or export and apply the diff),
  then remove the worktree. The working copy must end up holding the changes.

## Running code and tests.

Use the `uv` and `.venv` virtual environments to run code and tests — the
project `.venv` is the same interpreter PyCharm/Junie use by default
(`.venv/bin/python -m pytest ...`).

PyCharm/Junie run configurations also load the project `.env` file (via the
EnvFile/built-in dotenv support), which supplies secrets and config such as
`OPENAI_API_KEY`, `GEMINI_API_KEY`, and the `KAVALAI_*` DB
settings. `conftest.py` does **not** auto-load `.env`, so when running from the
shell you must load it yourself to match the PyCharm environment — otherwise
integration tests gated on these keys silently skip:

```bash
# Run tests with .env loaded, using the project venv (mirrors PyCharm/Junie)
.venv/bin/dotenv run -- .venv/bin/python -m pytest

# Equivalently, with uv:
uv run --env-file .env pytest
```

Avoid `set -a && source .env && set +a` — changing shell option state defeats
Claude Code's static command analysis and forces a manual permission prompt.

`.env.example` is the committed template and must list every variable the code
reads, and nothing else — `tests/test_config_drift.py` checks both directions,
so adding a `getenv` without documenting it fails the suite.

## Packaging & Extras

`pyproject.toml` keeps the base install small and pyodide-compatible (no
greenlet, no native extensions beyond the prebuilt pyodide packages). There are
only four extras:

- `common` — everything else: provider SDKs, RAG/embeddings, Postgres drivers,
  MCP, the REST/SSE servers, the bundled web tools. The normal install.
- `common_web` — what the core additionally needs under Pyodide/WebLLM
  (`pyodide-http`).
- `test` — test tooling; pulls in `kavalai[common]`, so CI is `uv sync --extra test`.
- `docs` — Sphinx, the theme and the notebook kernel.

Install hints in error messages point at `kavalai[common]`; keep them in sync
when the layout changes. Run `uv lock` after editing dependencies.

## Key Directories

| Path | Purpose |
|------|---------|
| `kavalai/` (top level) | Core SDK/runtime modules: `agent.py`, `agent_service.py`, `db.py`, `server.py`, `run_context.py`, `functionkernel.py`, … |
| `kavalai/eval/` | Evaluation against a **running** agent server: `base.py` (`AgentEvaluator`, `EvalResult`), `simple_evaluator.py` (literal matchers), `judge_evaluator.py` (a model grades a plain-language criterion), `eval_runner.py` (YAML cases + the `kavalai-eval` console script) |
| `kavalai/rag/` | RAG services: `BaseRagService` interface, `PostgresRagService` (pgvector), `SqliteRagService` (sqlite-vector file index, browser/WASM-portable) |
| `kavalai/llm_clients/` | Native LLM clients (OpenAI, Gemini, Anthropic, Ollama, in-browser WebGPU) behind one streaming interface; `registry.py` holds the name→backend registries that make that set extensible |
| `kavalai/tools/` | Utility tools (browser, RSS, web search, HTTP); `webtools/crawl4ai.py` provides both `crawl_url` and a key-free `web_search` (scrapes the DuckDuckGo HTML endpoint) |
| `kavalai/migrations/` | Alembic migration sets: `agents` (runtime + history tables) and `backoffice`; autogenerated from the ORM models (single source of truth) |
| `kavalai/workflow/` | Workflow engine v2: `engine.py`, `models.py`, `builder.py`, `expressions.py`, write-behind `tasklog/` backends; persistence goes through `AgentService` (`agent_service=` engine param) |
| `backoffice/` | FastAPI backoffice API + DB + project service |
| `frontend/` | Angular UI (Tailwind CSS + DaisyUI): `src/app/models` (interfaces), `src/app/services` (API/state), `src/app/components` (by feature), `src/styles/theme.css` (DaisyUI/Tailwind theme vars) |
| `tests/` | Pytest backend test suite for the **library**; an example's tests live in its own folder under `examples/` |
| `examples/` | Runnable usage examples; `green_village/` is the RAG evaluation example — the chatbot served on port 25000 by `green_village_support_in_memory.py`, with its 64 cases in `eval_cases.yaml`; `bakery/` is the YAML-workflow example — `assistant.yaml` is the graph, `bakery.py` the catalogue/models/tools it calls (English only; the parse step maps the customer's words onto the underscored `CATALOGUE` keys, so there is no alias table in Python, and the order book is always an in-memory list), and `bakery_in_memory.py` (port 25100) / `bakery_real_db.py` (port 25001) differ only in where the agent's *sessions* are recorded — each carries its own `build_engine`/`create_app` — with 26 cases in `eval_cases.yaml`; `chat_client/` is the terminal chat client for any agent server speaking `user_message` → `agent_response` (other reply fields shown as truncated JSON; tested by `test_chat_client.py` beside it); `business_info_agent/` is the web-research example — `business_info.py` is the workflow (DuckDuckGo search → crawling agent → summary), `business_info_in_memory.py` (port 25200) serves it for the 10 cases in `eval_cases.yaml`, and `research_companies.py` runs a company list through it from the terminal (`--no-db` to record nothing); both record into in-memory SQLite only — this example has no real-database variant; `support_agent/support_agent.yaml` is the branching-DAG example the docs and the agent-server tutorial load |
| `docs/` | Sphinx documentation (`sphinx_immaterial`): `tutorials/` (incl. notebook symlinks into `notebooks/`, plus `architecture.rst`), `guides/` (concepts, incl. `data_model.rst`), `reference/` (YAML, bundled tools, `providers.rst` for the model/embedding/RAG provider registries and their model catalogues, env vars), `cookbook/`, `deploy/`, `ui/`, autogenerated `api/`, and `todo.rst` for open maintainer questions. `_ext/` holds build-time generators (`workflow_svgs.py`, `architecture_svg.py`); `_extra/` is copied to the site root and holds `llms.txt` |
| `notebooks/` | Jupyter notebooks for documentation and tutorials — the **source of truth** for the LLM clients, agents, workflows, RAG and streaming tutorials |
| `dockerfiles/` | Docker images + entrypoints for agent server and backoffice |

## Key Files

- `kavalai/agent_service.py` — `AgentService`: all runtime persistence (agents, sessions, runs, chat history, tasks, model-call stats) over a plain `async_sessionmaker`; sessions reusable by caller-supplied `external_id`
- `kavalai/db.py` — ORM models + `DatabaseManager`; `model_call_stats` records tokens only (`cached_prompt_tokens` / `reasoning_tokens` included) — **no cost column**, deliberately: providers return tokens not money, and cached input is priced differently enough that a derived total would be wrong rather than stale (see `docs/guides/observability.rst`); `get_sqlite_compat_sessionmaker()` runs `AgentService` over the sync SQLite engine via `AsyncSessionShim` (Pyodide/browser, where greenlet/aiosqlite don't exist)
- `kavalai/workflow/engine.py` — core workflow engine (`WorkflowEngine.from_yaml_path`, YAML → execution); `run_stream()` yielding `WorkflowStreamEvent`s is the single execution path and `run()` just drains it. Node streaming is opt-in (`stream_output`, `stream_delta`, agent-only `stream_instructions` / `stream_partials`), settable from YAML and `WorkflowBuilder`. **One engine serves many concurrent runs**: per-run state (the `TokenAccumulator`) lives on `RunContext` and must be forwarded by `_branch_context`; engine-level state (the `FunctionKernel` and its MCP sessions) is opened by `await engine.connect()` and released by `await engine.aclose()`, never per run. `rag_services=` takes a bare `BaseRagService` (stored as `"default"`) or a dict; a `rag_query` node resolves *node → graph `rag_service` → `"default"`*, passed services beating registered ones, and an unresolvable name fails when the engine is constructed
- `kavalai/workflow/models.py` — Pydantic data models for workflows: shared building blocks (`ArgumentInfo`, `RestServer`, `McpServer`, `PythonFunction`, `TemplateModel`, `WorkflowException`) and the v2 graph (`WorkflowGraph`, nodes); a graph has exactly one `start` node (derived `start`/`output_type` properties, no `start` field) and all `end` nodes must share one output data type; `RagQueryNode` (`type: rag_query`) is read-only — it reaches only `BaseRagService.query` — and is exempt from the "output must be declared in `data_types`" rule via `_NODES_WITH_OWN_OUTPUT_TYPE`, because Kaval.AI owns that node's output shape. A workflow names a *registration*, never a Python path or a connection string: a provider containing `.` and a `service` containing `://` are both rejected at load
- `kavalai/agent.py` — modular planning agent (uses the native LLM clients)
- `kavalai/functionkernel.py` — tool registration and execution (REST, MCP, Python); unified tool URI format `protocol://[name|module].function_name(args: type) -> return_type`; duplicate tool/server names raise `WorkflowException`; `get_tool_descriptions()` takes an `allowed_tools` filter (`None` → all tools, `[]` → none, `"*"` → all, `proto://server.*` allows a whole server) and connects any unconnected MCP server first. `Agent(allowed_tools=…)` applies it to both the described tools and the tool calls it will execute; `AgentNode.allowed_tools` is `Optional[list[str]]` and the engine passes it through verbatim, so YAML and Python agree. MCP sessions are opened by `connect_mcp_servers()` and released by `close()` — both are kernel lifetime, never per run. Tool results: a declared Pydantic model is validated, everything else is wrapped as `.result`, and a mismatch raises `FunctionKernelException`
- `kavalai/run_context.py` — RunContext model and context resolution helpers
- `kavalai/workflow/render.py` — renders a workflow graph to an SVG diagram (`render_workflow_svg`); used by the docs build (`docs/_ext/workflow_svgs.py`) and the backoffice `POST /workflows/render-svg` endpoint. The frontend `workflow-graph` component displays that backend SVG.
- `kavalai/migrate_db.py` — Alembic migration runner (`python -m kavalai.migrate_db {agents|backoffice}`); only its `main()` reads env vars
- `backoffice/server.py` — FastAPI backoffice API (agents, sessions, stats, projects)
- `kavalai/server.py` — Agent REST server (sync + SSE streaming)
- `kavalai/llm_clients/registry.py` — name→backend registries for LLM clients, embedding clients and RAG services. `register_llm_provider` / `register_embedding_provider` / `register_rag_service` accept a class, a **dotted path** (imported on first use) or a callable, plus `**defaults` bound at registration; duplicates raise, `replace=True` warns. Built-ins are registered as dotted strings so `import kavalai` still works under Pyodide with no provider SDK installed — never register a built-in as an imported class. Lookup is exact-then-provider-prefix, so `mycorp/model` can be pinned separately from `mycorp`. `make_client` / `make_embedding_client` / `make_rag_service` are lookups over these. The pieces a third party needs to write a client — `BaseLlmClient`, `BaseEmbeddingClient` and `ensure_user_turn` — are exported from `kavalai`, and the worked examples live in `notebooks/llm_clients.ipynb` ("Bringing your own provider") and `docs/cookbook/index.rst`
- `kavalai/rag/base.py` — `BaseRagService` declares three tiers: six required methods, two optional (`count_entries` / `iter_entries`, guarded by `supports()`), two defaulted. `similarity` is **higher-is-better cosine** (`1 - distance`) on every backend, and `query`/`query_batch` take `include_content`. Backends may add whatever else their store does well — `tests/rag/test_conformance.py` runs the declared contract against all of them, so add a backend there rather than only in its own test file
- `kavalai/llm_clients/llm_client.py` — high-level LLM client interface

## Backend Testing

```bash
# Run all tests
pytest

# Run with coverage
pytest --cov=kavalai tests/

# Run specific test file
pytest tests/test_functionkernel.py
```

- Framework: `pytest` + `pytest-asyncio`
- Target: **100% coverage** for new and modified code
- Keep tests for a single source file in a single test file (e.g. `agent.py` → `test_agent.py`)
- `tests/` holds tests for the library only. An example's tests live **in the example folder** beside what they test (`examples/chat_client/test_chat_client.py`, `examples/business_info_agent/test_business_info_agent.py`); `testpaths = ["tests", "examples"]`, so a bare `pytest` still runs both. They cannot use `tests/conftest.py` fixtures — an example's test is expected to stand on its own
- At the end of every coding task, update the relevant tests and run them to catch regressions and confirm new behavior; always run tests before submitting a task. Run only failing tests to save time when you know which ones fail
- Mock MCP servers used in tests live in `tests/helpers/`

## Frontend Testing

```bash
cd frontend && npm test -- --watch=false
cd frontend && npm test -- --watch=false --code-coverage
```

- Framework: Jasmine + Karma
- Update mocks when service interfaces change

## Documentation

- Sphinx builds from `docs/`; `sphinx-build -b html docs docs/_build/html` must
  finish with **zero warnings**.
- Five tutorials are notebooks symlinked from `docs/tutorials/` into
  `notebooks/`: `llm_clients`, `agents`, `workflow`, `rag`, `streamer`. Edit the
  notebook, never the symlink, and **re-execute it** — `nb_execution_mode` is
  `"off"`, so the docs render whatever outputs are stored in the file. Examples
  must be run against live providers, not written by hand.
- The remaining pages are `.rst`. Every code example in them should be executed
  before it is committed; paste the real output underneath.
- Cross-reference from notebooks with MyST roles (`` {doc}`agents` ``,
  `` {class}`~kavalai.Agent` ``), and from `.rst` with `:doc:` / `:class:`.
- `README.md` deep-links to anchors on the tutorial pages
  (e.g. `tutorials/llm_clients.html#streaming-responses`). Renaming a heading
  breaks them, and `README.md` is not to be edited — keep the headings stable.
- WebLLM / `browser/` examples belong **only** in
  `docs/tutorials/run_in_browser.rst`, which also hosts the live chat widget
  (`docs/_includes/chatbot-demo.html`, extracted by `tests/test_chatbot_demo.py`).
- Anything found that needs a code change rather than a doc fix goes in
  `docs/todo.rst`.
- Keep code blocks within ~80 characters so the rendered page needs no
  horizontal scrollbar, and avoid elliptical comments (`# ...and a dozen more`)
  in examples — they read as missing code rather than as abbreviation.
- The architecture diagram is generated by `docs/_ext/architecture_svg.py` on
  every build; edit that module rather than the SVG.

## Coding Guidelines

- Use modern Angular control flow syntax: `@if`, `@for` — never `*ngIf` / `*ngFor`
- Prefer styles and components from `common.css`; use Tailwind CSS + DaisyUI for styling
- Refactor code blocks with distinct responsibilities into dedicated functions
- Do not update `README.md`
- Python tools must be decorated with `@kavalai.pythontool` and registered via `register_python_tool`
- Keep this `CLAUDE.md` current: after a task, update it for important changes to project structure, components, or workflow

## Evaluation & Acceptance Testing

- `kavalai/eval/` evaluates an agent server that is **already running**. It
  talks HTTP and discovers the agent's input/output Pydantic types from the
  server's OpenAPI spec (via `kavalai/client.py`), so it knows nothing about
  the engine, the workflow file or the database.
- `SimpleEvaluator` compares the answer with expected values — no model, same
  verdict every time. Matchers: `equals`, `contains` (substring for text,
  case-insensitive; membership for a list), `not_contains`, `regex`, `one_of`.
- `JudgeEvaluator` asks a judging model whether the answer meets a
  plain-language criterion, and reports the judge's reason when it does not.
  The judge client is built on first use, so a run of literal cases needs no
  API key.
- `eval_runner.py` reads a YAML file of cases (`type: simple` or
  `type: judge`) and runs them in order:
  `uv run --env-file .env kavalai-eval <cases.yaml> --host … --port … --tag …
  --auth user:password`. Exit `0` all passed, `1` a case failed, `2` the run
  never reached a verdict — CI needs the third to tell "the suite is broken"
  from "the agent is wrong".
- **A case file never names the server it grades.** There is no `base_url`
  key, `--port` is required and `AgentEvaluator` has no default base URL: the
  agent under evaluation belongs to the run, so the same cases can be pointed
  at a laptop, at staging, or at two model versions in turn.
- **`kavalai.eval` reads no environment variables** — only
  `eval_runner.py:main()` does. Everything else is an argument.
- Both evaluators are meant to be called straight from a unit test:
  `result = await evaluator.evaluate(inputs, expected)`, then assert on
  `result.passed` / `result.reason`.
- A judged case with no criterion is refused, in `EvalCase` and in the
  evaluator: judging against nothing passes on any answer at all. A suite key
  that is not recognised is refused too — a silently ignored key is a case
  that never ran.
- A failing agent call fails its case with a reason rather than raising, so one
  broken case cannot end a run. Each case runs in a fresh session, recorded
  under `external_id = "eval:{tag}:{case}"` (`eval:{case}` without `--tag`).
  `--tag` names the run — a model version, a prompt variant, a build — and is
  what makes two runs comparable in the agent database afterwards; the `eval:`
  prefix is what the backoffice sessions page filters on. Sessions are written
  only when the agent server has an `AgentService`, and the evaluators behave
  identically when it does not.
- `examples/green_village/eval_cases.yaml` is the worked example: 64 cases,
  literal ones for the facts and judged ones for the comparisons and refusals.
- `examples/business_info_agent/eval_cases.yaml` is the open-web one: 10 cases
  against an agent that searches and crawls live pages, so they assert only
  what a company keeps stating about itself — and, in
  `unknown_business_is_not_invented`, that a business the web has never heard
  of comes back as null fields rather than a fluent invention.
- `examples/bakery/eval_cases.yaml` is the side-effecting one: 26 cases against
  a workflow that writes orders. It can be mostly literal because
  `compose_reply` stamps `status` / `order_id` / `missing` onto the answer from
  what Python decided — the model only ever writes `subject` and `body`.

## Database Migrations

- Alembic, two sets: `kavalai/migrations/agents/` and `kavalai/migrations/backoffice/`; the ORM models (`kavalai/db.py`, `kavalai/backoffice/db.py`) are the single source of truth
- Models are **schema-less**; the target schema is applied per-engine via `schema_translate_map` (`DatabaseManager.get_sessionmaker(..., schema=...)`). Library code never reads env vars — only entry-point `main()`s do
- New revision: change the models, then autogenerate against an empty scratch DB with the set's env (`config.attributes["connection"]`/`["schema"]`); parity tests in `tests/test_migrate_db.py` fail if models and revisions diverge
- Raw SQL bypasses `schema_translate_map` — qualify the schema explicitly (see `_translated_schema_prefix` in the agents initial revision, `SET search_path` in `PostgresRagService`). So does **reflection**: `op.batch_alter_table` reflects before altering, so pass the translated schema explicitly (`_target_schema()` in revision 0003) or the ALTER looks for the table in `public`
- Browser/Pyodide SQLite never runs Alembic: `DatabaseManager.init_sqlite*` uses `create_all` + a `PRAGMA user_version` stamp (`SQLITE_SCHEMA_VERSION`); bump it on schema changes so stale browser DBs are wiped and recreated

## Workflow Execution

1. A YAML definition is loaded (`WorkflowEngine.from_yaml_path`).
2. `SchemaParser` generates Pydantic models for workflow input/output.
3. The engine executes the tasks (prompts, tools) with pluggable `storage`/`task_logger` backends.
4. `AgentService` records sessions, runs, tasks and model-call stats in the agent database.

## Backoffice Security & Project Isolation

- Authentication via Google OAuth (configured in `kavalai/backoffice/server.py`).
- Role-based access per project (`owner`, `viewer`); membership is verified on API calls.
- The last owner of a project cannot be removed or demoted.
- The backoffice manages multiple **projects**; each project points to its own agent database (`db_host`/`db_port`/`db_name`/`db_schema` on the `projects` row). Users have an `active_project_id` used as the default project; the global header project selector synchronizes it across pages.
- `active_project_id` must always point at a project the user can still reach, or every project-scoped endpoint answers 403. `db.resolve_active_project_id()` repairs a stale value (falling back to another of the user's projects, or `None`) and runs on login and on `/user/get_details`; deleting a project or removing a member clears it, backed by `ON DELETE SET NULL` on the foreign key.

## Dangerous Commands

When the user denies a delete/remove or other destructive command, ask them to perform it manually rather than retrying.

## Tech Stack

- **Backend**: Python, FastAPI, SQLAlchemy (Async), Pydantic
- **Frontend**: Angular, Tailwind CSS, DaisyUI
- **Database**: PostgreSQL (multiple schemas)
- **Workflows**: YAML definitions processed by `Workflow` class
- **Security**: `ruff`, `yamllint`, `bandit`, `gitleaks` pre-commit hooks

## Docker Commands

```bash
# Backoffice
docker run ... backoffice-migrations
docker run ... backoffice-server   # needs KAVALAI_BO_DB_URI, KAVALAI_BO_DB_SCHEMA

# Agent server
docker run ... agent-server        # needs KAVALAI_AGENT_WORKFLOW_PATH, KAVALAI_DB_URI, KAVALAI_DB_SCHEMA
# optional HTTP basic auth: KAVALAI_AGENT_BASIC_AUTH_USER, KAVALAI_AGENT_BASIC_AUTH_PASSWORD

# Migrations
docker run ... agent-migrations
```

`docker-compose.yml` also provides a `torproxy` service (Tor + Privoxy proxy, `dperson/torproxy`)
and a `crawl4ai` service (`unclecode/crawl4ai`, REST API on port 11235).
