Text API
========

:mod:`kavalai.text` turns HTML and markdown into the chunks a RAG index
stores. It uses the standard library only, so it also runs under Pyodide, and
it is not imported by ``import kavalai``. For the parameters and a worked
example, read :doc:`/reference/text`.

.. automodule:: kavalai.text
